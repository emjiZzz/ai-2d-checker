{
  "name": "@ai-2d-checker/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript type definitions and contracts for the AI-2D-Checker monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "lint": "eslint \"src/**/*.ts\""
  },
  "devDependencies": {
    "@ai-2d-checker/config": "workspace:*",
    "rimraf": "^6.1.3",
    "typescript": "^5.4.5"
  }
}
