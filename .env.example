# ==============================================================================
# AI-2D-Checker — Environment Template
# Copy this file to `.env` in the root folder and configure values.
# ==============================================================================

# MongoDB Configuration
# For local offline-first usage, defaults should point to local MongoDB Community Server.
MONGO_URI=mongodb://localhost:27017
MONGO_FALLBACK_URI=mongodb://localhost:27017
MONGO_DB_NAME=ai_2d_checker
ENABLE_DB_AUTO_SYNC=true
DB_AUTO_SYNC_INTERVAL_SEC=60

# Initial Seeding Credentials (Override in production/cloud)
SEED_ADMIN_PASSWORD=admin123
SEED_ENGINEER_PASSWORD=engineer123

# Network & Host Binding Configuration
# Set SIDECAR_PORT to 0 to dynamically allocate a port at startup (or use PORT on cloud).
SIDECAR_PORT=8080
SIDECAR_HOST=127.0.0.1
SIDECAR_LOG_LEVEL=INFO
ALLOWED_HOSTS=
CORS_ORIGINS=

# API Security Token (Set fixed token for LAN/cloud deployments)
API_TOKEN=

# AI Core Configuration
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
GEMINI_MODEL_PRO=gemini-2.5-flash
GEMINI_MODEL_FLASH=gemini-flash-latest
GEMINI_MODEL_FALLBACK=gemini-flash-latest

# OpenAI API Configuration
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
OPENAI_MODEL=gpt-5.4

# Feature Flags
ENABLE_LLM_SUMMARY=false

# Storage & File Handlers
# Absolute or relative path to the git-ignored local storage hierarchy.
STORAGE_ROOT=./storage
MAX_FILE_SIZE_MB=500

# ODA File Converter Installation Path
# Required for converting proprietary DWG drawings to open DXF on Windows.
# Download from: https://www.opendesign.com/guestfiles/oda_file_converter
ODA_CONVERTER_PATH=C:/Program Files/ODA/ODAFileConverter_25.12.0/ODAFileConverter.exe
